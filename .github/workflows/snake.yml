name: Generate Snake

# Corre automáticamente cada 6 horas
on:
  schedule:
    - cron: "0 */6 * * *"  # Ejecuta cada 6 horas
  workflow_dispatch:  # Permite ejecutarlo manualmente

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Paso 1: Clona el repositorio
      - name: Checkout
        uses: actions/checkout@v3

      # Paso 2: Paso de depuración para confirmar si el flujo de trabajo fue activado
      - name: Debugging step
        run: echo "Workflow triggered manually."

      # Paso 3: Generar la animación de la serpiente
      - name: Generate Snake
        uses: Platane/snk@v3
        with:
          github_user_name: rafaelchuco

      # Paso 4: Asegurarse de que el directorio 'dist' exista
      - name: Create dist directory
        run: mkdir -p dist

      # Paso 5: Listar los archivos en el directorio 'dist' para depuración
      - name: List files in dist directory
        run: ls -la dist

      # Paso 6: Crear un archivo de salida manualmente para la ruta de los archivos generados
      - name: Set output paths
        run: |
          echo "OUTPUT_SVG=dist/github-contribution-grid-snake.svg" >> $GITHUB_ENV
          echo "OUTPUT_GIF=dist/github-contribution-grid-snake.gif" >> $GITHUB_ENV

      # Paso 7: Publicar los archivos generados en la rama 'output'
      - name: Deploy to output branch
        uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
